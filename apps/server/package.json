{
  "name": "@eternal-blocks/server",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker + Durable Object (SQLite) authoritative server for Eternal Blocks.",
  "type": "module",
  "exports": {
    "./coordinator": "./src/coordinator.ts",
    "./store": "./src/store.ts"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "check:deploy": "wrangler deploy --dry-run --outdir dist-server",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@eternal-blocks/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250823.0",
    "wrangler": "^4.30.0"
  }
}
